"""Tests for the aCRF parser.

A package rather than a bare directory so `tests/msg.py` and
`tests/msg_pipeline.py` can import one another, and so `scripts/score_msg.py`
can import both from outside pytest.
"""
